import React, { FC } from 'react';

export interface FundInfoItemProps {
  name: string;
  up?: number;
  down?: number;
}

export const FundInfoItem: FC<FundInfoItemProps> = (props) => {
  return <div></div>;
};
